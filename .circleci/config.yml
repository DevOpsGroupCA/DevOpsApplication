version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:20.17.0
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Build Application
          command: npm run build
workflows:
  build_workflow:
    jobs:
      - build
